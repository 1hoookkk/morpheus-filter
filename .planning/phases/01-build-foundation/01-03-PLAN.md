---
phase: 01-build-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "VST3 passes pluginval validation"
    - "Standalone application launches and displays window"
    - "Audio passes through plugin unchanged in bypass mode"
  artifacts:
    - path: "build/TRENCH_artefacts/Release/VST3/TRENCH.vst3"
      provides: "Validated VST3 plugin"
    - path: "build/TRENCH_artefacts/Release/Standalone/TRENCH.exe"
      provides: "Working standalone application"
  key_links:
    - from: "VST3 binary"
      to: "DAW plugin list"
      via: "pluginval validation"
      pattern: "All tests passed"
    - from: "bypass parameter"
      to: "audio passthrough"
      via: "processBlock bypass path"
      pattern: "bypassed.*return"
---

<objective>
Validate that the fixed plugin builds work correctly: VST3 loads, Standalone runs, and audio passes through.

Purpose: Verify requirements BUILD-01, BUILD-02, BUILD-03 are complete. This is the final validation before marking Phase 1 complete.

Output: Confirmed working plugin ready for Phase 2 (DSP Engine).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-foundation/01-02-SUMMARY.md
@Source/PluginProcessor.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate VST3 with pluginval</name>
  <files>none</files>
  <action>
Run pluginval with strictness level 5 (minimum for commercial compatibility):
```bash
pluginval --strictness-level 5 --validate "C:/Users/hooki/do-it/build/TRENCH_artefacts/Release/VST3/TRENCH.vst3"
```

Expected output should include:
- "Starting test: Create plugin instance"
- "Starting test: Plugin editor"
- "Starting test: Process audio"
- "All tests passed"

If pluginval is not available:
1. Download from https://github.com/Tracktion/pluginval/releases
2. Or use JUCE AudioPluginHost from build/_deps/juce-src/extras/AudioPluginHost/

Record the full output for documentation.
  </action>
  <verify>pluginval exits with code 0 and reports "All tests passed" (or equivalent success message)</verify>
  <done>VST3 passes automated validation - confirms BUILD-02</done>
</task>

<task type="auto">
  <name>Task 2: Test Standalone launch</name>
  <files>none</files>
  <action>
1. Launch the Standalone application:
```bash
start "" "C:/Users/hooki/do-it/build/TRENCH_artefacts/Release/Standalone/TRENCH.exe"
```

2. Wait 5 seconds to confirm it doesn't crash immediately

3. Check the process is running:
```bash
tasklist | grep -i TRENCH
```

4. Take note of:
   - Window appears with TRENCH UI
   - No error dialogs
   - Application remains responsive

5. Close the application

If it crashes, capture the error message or check Windows Event Viewer.
  </action>
  <verify>TRENCH.exe process runs for 5+ seconds without crash, window is visible</verify>
  <done>Standalone launches successfully - confirms BUILD-03</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Fixed TRENCH VST3 plugin and Standalone application with:
- Constructor safety (try/catch)
- Both build targets working
- pluginval validation passing
  </what-built>
  <how-to-verify>
**VST3 in DAW Test:**
1. Copy VST3 to DAW plugin folder (or use DAW's plugin scan):
   `C:\Program Files\Common Files\VST3\TRENCH.vst3`
   OR scan from: `C:\Users\hooki\do-it\build\TRENCH_artefacts\Release\VST3\`
2. Open your DAW (FL Studio, REAPER, Ableton, etc.)
3. Rescan plugins if needed
4. Insert TRENCH on a track
5. Verify plugin window opens
6. Enable BYPASS button in plugin
7. Play audio through the track
8. Confirm audio passes through unchanged (bypass should be transparent)

**Standalone Test:**
1. Launch: `C:\Users\hooki\do-it\build\TRENCH_artefacts\Release\Standalone\TRENCH.exe`
2. Verify window appears with controls (Morph, Q, Drive, Mix, Output, Bypass)
3. Move sliders - they should respond
4. Close and reopen - should work consistently

**Expected Results:**
- DAW shows TRENCH in plugin list
- Plugin window displays correctly
- Audio plays through when bypassed
- No crashes during any operation
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe specific issues encountered</resume-signal>
</task>

</tasks>

<verification>
1. pluginval reports success (all tests passed)
2. Standalone launches without crash
3. User confirms VST3 loads in actual DAW
4. User confirms audio passthrough works in bypass mode
</verification>

<success_criteria>
- BUILD-01: Plugin compiles without errors (verified in Plan 02)
- BUILD-02: VST3 loads successfully in DAW (user verified)
- BUILD-03: Standalone app launches and runs (user verified)
- Audio passthrough: Audio passes through unchanged when bypassed (user verified)
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-foundation/01-03-SUMMARY.md`
</output>
